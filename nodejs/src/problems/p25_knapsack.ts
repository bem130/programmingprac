// 問題25: 0-1 ナップサック問題 (難易度: ★★★★☆)
// items は [重さ, 価値] のペアの配列。容量 capacity での最大価値を返す。
export function knapsack(capacity: number, items: [number, number][]): number {
  throw new Error("todo"); // ここを実装してください
}
